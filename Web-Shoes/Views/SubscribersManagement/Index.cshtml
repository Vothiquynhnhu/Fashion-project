@model IEnumerable<Web_Shoes.Entity.Subscribers>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<div id="page-wrapper">
    <div id="page-inner">
        <div class="row">
            <div class="col-lg-12">
                <h2>Users:</h2>
            </div>
        </div>
        <!-- /. ROW  -->
        <hr />
        
        <div>


            <table class="table">
                <thead>
                    <tr>
                        <th>
                            @Html.DisplayNameFor(model => model.Subs_Id)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.Subs_Mail)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.Subs_TimeUpdate)
                        </th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.Subs_Id)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Subs_Mail)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Subs_TimeUpdate)
                            </td>
                            <td>
                                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
                            </td>
                        </tr>
                    }
                </tbody>
            </table>

        </div>

    </div>
    <!-- /. PAGE INNER  -->
</div>



